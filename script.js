const body=document.body;const themeBtn=document.querySelector('#themeBtn');const simulateBtn=document.querySelector('#simulateBtn');const startBtn=document.querySelector('#startBtn');const readiness=document.querySelector('#readiness');const load=document.querySelector('#load');const stateLabel=document.querySelector('#stateLabel');const signals=[['recovery',document.querySelector('#recoveryValue'),document.querySelector('#recoveryMeter')],['energy',document.querySelector('#energyValue'),document.querySelector('#energyMeter')],['performance',document.querySelector('#performanceValue'),document.querySelector('#performanceMeter')]];const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.16});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));themeBtn.addEventListener('click',()=>body.classList.toggle('light'));simulateBtn.addEventListener('click',()=>{const values=signals.map(()=>Math.floor(58+Math.random()*38));signals.forEach(([,label,meter],index)=>{label.textContent=values[index];meter.style.setProperty('--value',`${values[index]}%`)});const score=Math.round(values.reduce((sum,value)=>sum+value,0)/values.length);readiness.textContent=score;load.textContent=`${Math.max(52,Math.min(92,score-8))}%`;stateLabel.textContent=score>=82?'READY':score>=70?'BALANCED':'RECOVER';simulateBtn.textContent='Day recalculated';setTimeout(()=>simulateBtn.textContent='Simulate new day',1200)});document.querySelectorAll('#exerciseList button').forEach(button=>button.addEventListener('click',()=>button.classList.toggle('done')));let active=false;startBtn.addEventListener('click',()=>{active=!active;startBtn.textContent=active?'Session active · 00:01':'Start session';startBtn.setAttribute('aria-pressed',String(active))});